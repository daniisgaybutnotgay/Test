for host in google.com cloudflare.com github.com raw.githubusercontent.com pypi.org npmjs.com; do
  code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 3 https://$host/ 2>/dev/null)
  echo "$host: HTTP $code"
done
